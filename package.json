{
  "name": "brreg-snap",
  "version": "1.3.1",
  "description": "Nettleserutvidelse (Firefox og Chrome) som henter bedriftsinfo fra Brønnøysundregistrene med ett klikk.",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.33.0",
  "engines": {
    "node": "^22.13 || ^24 || >=26"
  },
  "scripts": {
    "build": "pnpm run build:firefox",
    "build:firefox": "vite build --mode firefox",
    "build:chrome": "vite build --mode chrome",
    "dev": "pnpm run dev:firefox",
    "dev:firefox": "pnpm run build:firefox && web-ext run --source-dir dist-firefox",
    "dev:chrome": "pnpm run build:chrome && web-ext run -t chromium --source-dir dist-chrome",
    "watch": "vite build --watch --mode firefox",
    "package": "pnpm run package:firefox",
    "package:firefox": "pnpm run build:firefox && web-ext build --source-dir dist-firefox --artifacts-dir web-ext-artifacts --overwrite-dest --ignore-files \"**/*.map\" \"icons/README.md\"",
    "package:chrome": "pnpm run build:chrome && web-ext build --source-dir dist-chrome --artifacts-dir web-ext-artifacts --filename \"brreg-snap-chrome-{version}.zip\" --overwrite-dest --ignore-files \"**/*.map\" \"icons/README.md\"",
    "package:source": "node scripts/package-source.mjs",
    "lint:ext": "pnpm run lint:ext:firefox",
    "lint:ext:firefox": "web-ext lint --source-dir dist-firefox",
    "lint:ts": "eslint \"src/**/*.ts\"",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc -p tsconfig.json && tsc -p tests/tsconfig.json && tsc -p tsconfig.node.json"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@types/firefox-webext-browser": "^143.0.0",
    "@types/node": "^24.13.5",
    "eslint": "^10.10.0",
    "happy-dom": "^20.14.5",
    "pngjs": "^7.0.0",
    "typescript": "^6.0.3",
    "typescript-eslint": "^8.70.0",
    "vite": "^8.3.0",
    "vitest": "^5.0.1",
    "web-ext": "^10.6.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/ikkeseb/brreg-snap.git"
  },
  "license": "MIT"
}
